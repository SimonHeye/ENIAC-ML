# ENIAC-ML

# The training file is main.py
You can set the method parameter to decide which model to run(
pipeline_MAML is the complete active meta-learning model,pipeline is without active learning)

# The testing file is testrecallmodel.py
